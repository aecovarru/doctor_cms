<% @page_title = "Doctor Type" %>

<%= link_to("<<Back to Menu", {:controller => 'access', :action => 'index'}, :class => 'back-link') %>

<div class="menu">
	<h2><%= @page_title %></h2>
	<div class = "identity">Logged in as: <%= session[:username] %></div>
	<ul>
		<% @doctors.each do |doctor| %>
		<li><%= link_to(doctor, :controller => 'accounts', :action => 'state', :doctor => doctor) %></li>
		<% end %>
	</ul>
</div>