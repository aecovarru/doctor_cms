<% @page_title = session[:admin] ? "Admin Menu" : "Menu" %>

<div class="menu">
	<h2><%= @page_title %></h2>
	<div class = "identity">Logged in as: <%= session[:username] %></div>
	<ul>
		<li><%= link_to("Manage Accounts", :controller => 'accounts', :action => 'doctor') %></li>
		<% if session[:admin] %>
		<li><%= link_to("Manage Admin Users", :controller => 'admin_users') %></li>
		<% end %>
		<li><%= link_to("Email", :controller => 'accounts', :action => 'email')%></li>
		<li><%= link_to("Logout", :action => 'logout') %></li>
	</ul>
</div>